spring:
  datasource:
    # Defaults match the docker-compose Postgres published on localhost:5432
    # (cbrsx_db / postgres). Always override via env in real deployments.
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/cbrsx_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}
  jpa:
    database-platform: ${SPRING_JPA_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
    hibernate:
      ddl-auto: validate
    show-sql: false

management:
  endpoint:
    health:
      show-details: never

logging:
  level:
    com.cbrsx: INFO
    org.hibernate.SQL: WARN

# API explorer is disabled under prod unless explicitly opted in
# (set CBRSX_SWAGGER_ENABLED=true only on trusted/internal deployments)
springdoc:
  api-docs.enabled: ${CBRSX_SWAGGER_ENABLED:false}
  swagger-ui.enabled: ${CBRSX_SWAGGER_ENABLED:false}

cbrsx:
  security:
    fail-closed: true
